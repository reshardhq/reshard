{
  "name": "agentchat",
  "private": true,
  "scripts": {
    "desktop": "pnpm --filter desktop tauri dev",
    "dev": "pnpm --filter desktop dev",
    "web": "pnpm --filter rebeam-web dev",
    "web:build": "pnpm --filter rebeam-web build"
  }
}
